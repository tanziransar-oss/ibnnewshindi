"use client";

import React from "react";

interface AdsPlaceholderProps {
  slotType: "header-banner" | "sidebar-box" | "in-content" | "sticky-footer";
}

export const AdsPlaceholder: React.FC<AdsPlaceholderProps> = () => {
  return null;
};

export default AdsPlaceholder;

